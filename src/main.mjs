/**
 * main module
 * @module main
 * @see module:ui
 * @file First and only module explicitly loaded from the index.html.
 *       Controls all other processes.
 * @author Luca Leon Happel
 */

import * as UI from './ui.mjs';

UI.init();
